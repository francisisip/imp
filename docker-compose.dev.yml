version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NEXTAUTH_URL=http://localhost:3000/api/auth
      - NEXTAUTH_SECRET=ePUNT5U1QqpqD47mFmJIactnLab94slGxiLD9pzOUhk=
      - MONGODB_URI=mongodb+srv://francisbawa:zKDOn9fMjwKzkCeM@project-deployment.cjfkjxm.mongodb.net/?retryWrites=true&w=majority&appName=project-deployment
      - MONGODB_DB=site-deployment
    depends_on:
      - mongo

  mongo:
    image: mongo:6.0
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data: